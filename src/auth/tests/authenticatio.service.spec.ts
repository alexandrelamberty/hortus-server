

describe('The AuthenticationService', () => {

  describe('when creating a cookie', () => {
    it('should return a string', () => {
      const userId = "1";
      expect(
        userId
      ).toEqual('1')
    })
  })
});